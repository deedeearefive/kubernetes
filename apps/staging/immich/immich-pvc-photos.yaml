apiVersion: v1
kind: PersistentVolumeClaim
namespace: immich
metadata:
  name: immich-photos
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 100Gi
