apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: immich-photos
  namespace: immich
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn-rc1
  resources:
    requests:
      storage: 150Gi
