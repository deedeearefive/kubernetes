apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: immich_base
namespace: immich
resources:
  - ../../base/immich
  - ./immich-pvc-photos.yaml
  - ./immich-cnpg-secrets.enc.yaml
patches:
  - path: immich-patch.yaml
    target:
      kind: HelmRelease
