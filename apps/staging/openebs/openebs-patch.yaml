apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: openebs
  namespace: openebs
spec:
  chart:
    spec:
      version: ">=4.3.3"
  test:
    enable: false
  values:
    mayastor:
    csi:
      node:
        # We don't want initContainters for Talos linux
        initContainers:
          enabled: false
    # We don't need local LVM engines
    engines:
      local:
        lvm:
          enabled: false
        zfs:
          enabled: false
