apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: openebs
  namespace: openebs
spec:
  chart:
    spec:
      version: ">=1.6.0"
  test:
    enable: false
  values:
    mayastor:
    csi:
      node:
        initContainers:
          enabled: false
    engines:
      local:
        lvm:
          enabled: false
        zfs:
          enabled: false
