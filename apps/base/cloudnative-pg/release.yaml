apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: cloudnative-pg-release
  namespace: cnpg-system
spec:
  releaseName: cloudnative-pg-release
  chart:
    spec:
      chart: cloudnative-pg
      sourceRef:
        kind: HelmRepository
        name: cloudnative-pg-repo
  interval: 1m
  install:
    remediation:
      retries: 3
  # Default values
  values:
